import 'package:flutter/widgets.dart';

class ContextsPage extends StatelessWidget {
  const ContextsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('contexts')),
    );
  }
}
